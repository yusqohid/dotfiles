# source = ~/.config/hypr/myColors.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=eDP-1, 1920x1080@60, 1920x0, 1 #horizontal
monitor=eDP-1, 1920x1080@60, 0x1080, 1 #vertikal
# monitor=eDP-1, disable
monitor=HDMI-A-1, 1920x1080@100, 0x0, 1
# monitor=HDMI-A-1, disable

###################
### MY PROGRAMS ###
###################

$terminal = foot --working-directory ~/Dev
# $terminal = kitty --working-directory ~/Dev
$fileManager = XDG_CURRENT_DESKTOP=kde dolphin
$menu = rofi -show drun
$webBrowser = firefox
$clipboard = ~/.local/bin/cliphist-rofi
$powermenu = ~/.config/rofi/powermenu.sh
$wallpaper = ~/.config/rofi/wallpaper-picker.sh
$toggle_waybar = ~/.local/bin/toggle_waybar.sh

#################
### AUTOSTART ###
#################

# exec-once = $terminal
# exec-once = waybar & hyprpaper & firefox
exec-once = swww-daemon
exec-once = waybar
exec-once = mako
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
# exec-once = hyprlock
# exec-once = ~/battery_warning.sh

# gtk-theme
exec-once = dbus-update-activation-environment --systemd DBUS_SESSION_BUS_ADDRESS DISPLAY XAUTHORITY WAYLAND_DISPLAY
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec-once = gsettings set org.gnome.desktop.interface gtk-theme 'adw-gtk3-dark'

#############################
### ENVIRONMENT VARIABLES ###
#############################

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = GTK_THEME,Adwaita-dark
env = PATH,$PATH:/home/yusqohid/.local/bin


#####################
### LOOK AND FEEL ###
#####################

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 4

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)
    #purple
    # col.active_border = rgba(6e4263ee) rgba(a96d98ee) 45deg
    # blue
    # col.active_border = rgba(9cd3f7ee)
    #green
    col.active_border = rgba(83a598ee)

    # col.active_border = rgba(904B47ee)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 2
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1
    inactive_opacity = 1

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = 0
        size = 10
        passes = 3
        ignore_opacity = true
        new_optimizations = true

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true
    # Animation curves

    bezier = linear, 0, 0, 1, 1
    bezier = liner, 1, 1, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.38, 0.04, 1, 0.07
    bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    bezier = softAcDecel, 0.26, 0.26, 0.15, 1
    bezier = md2, 0.4, 0, 0.2, 1 # use with .2s duration
    # Animation configs
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = windowsIn, 1, 3, md3_decel, popin 60%
    animation = windowsOut, 1, 3, md3_accel, popin 60%
    animation = border, 1, 10, default
    animation = borderangle, 1, 15, liner, loop
    animation = fade, 1, 3, md3_decel
    animation = layers, 1, 2, md3_decel, slide
    animation = layersIn, 1, 3, menu_decel, slide
    animation = layersOut, 1, 1.6, menu_accel
    animation = fadeLayersIn, 1, 2, menu_decel
    animation = fadeLayersOut, 1, 4.5, menu_accel
    animation = workspaces, 1, 7, menu_decel, slide
    animation = workspaces, 1, 2.5, softAcDecel, slide
    animation = workspaces, 1, 7, menu_decel, slidefade 15%
    animation = specialWorkspace, 1, 3, md3_decel, slidefadevert 15%
    animation = specialWorkspace, 1, 3, md3_decel, slidevert
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    middle_click_paste = false
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = true
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, A, exec, $toggle_waybar
bind = $mainMod, B, exec, $webBrowser
bind = $mainMod, C, togglefloating,
# bind = $mainMod, D,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F, fullscreen, 0
bind = $mainMod, G, exec, ghostty
# bind = $mainMod, H,
# bind = $mainMod, I,
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, M, exit,
# bind = $mainMod, N,
# bind = $mainMod, O,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, Q, killactive,
# bind = $mainMod, R,
# bind = $mainMod, S,
# bind = $mainMod, T,
# bind = $mainMod, U,
bind = $mainMod, V, exec, $clipboard
bind = $mainMod, W, exec, $wallpaper
bind = $mainMod, X, exec, XDG_CURRENT_DESKTOP=kde virt-manager
# bind = $mainMod, Y,
# bind = $mainMod, Z,
bind = ALT, SPACE, exec, $menu
bind = ALT, F4, exec, $powermenu

# bind = $mainMod SHIFT, A,
# bind = $mainMod SHIFT, B,
bind = $mainMod SHIFT, C, exec, $webBrowser --new-window https://chatgpt.com
bind = $mainMod SHIFT, D, exec, $webBrowser --new-window https://docs.google.com
bind = $mainMod SHIFT, E, exec, $webBrowser --new-window https://gemini.google.com
bind = $mainMod SHIFT, F, exec, $webBrowser --new-window https://www.figma.com
bind = $mainMod SHIFT, G, exec, $webBrowser --new-window https://github.com/yusqohid
bind = $mainMod SHIFT, M, exec, $webBrowser --new-window https://mail.google.com
bind = $mainMod SHIFT, P, exec, $webBrowser --new-window https://spotify.com
bind = $mainMod SHIFT, U, exec, $webBrowser --new-window https://smile.unp.ac.id
bind = $mainMod SHIFT, W, exec, $webBrowser --new-window https://web.whatsapp.com
bind = $mainMod SHIFT, Y, exec, $webBrowser --new-window https://youtube.com
# bind = $mainMod SHIFT, D,
# bind = $mainMod SHIFT, E,
# bind = $mainMod SHIFT, 8,
# bind = $mainMod SHIFT, 9,
# bind = $mainMod SHIFT, 0,

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

$d=[$ws|Navigation|Relative workspace]
bindd = $mainMod Control, Right, $d change active workspace forwards  , workspace, r+1
bindd = $mainMod Control, Left, $d change active workspace backwards , workspace, r-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Resize floating window width +20px
bind = $mainMod SHIFT, RIGHT, resizeactive, 80 0
bind = $mainMod SHIFT, LEFT, resizeactive, -80 0
bind = $mainMod SHIFT, UP, resizeactive, 0 -80
bind = $mainMod SHIFT, DOWN, resizeactive, 0 80

 # Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# window initial size
windowrulev2 = size 1200 750, class:com.mitchellh.ghostty
windowrulev2 = size 1200 750, class:foot
windowrulev2 = size 1200 750, class:kitty

# floating window
# windowrule = float,class:^(kitty)$,title:^(kitty)$
windowrule = float,class:com.mitchellh.ghostty

# opacity
# windowrulev2 = opacity 0.90 0.90, class:com.mitchellh.ghostty
# windowrulev2 = opacity 0.90 0.90, class:foot
# windowrulev2 = opacity 0.90 0.90, class:org.pwmt.zathura
# windowrulev2 = opacity 0.90 0.90, class:kitty
# windowrulev2 = opacity 0.95 0.95, class:dev.zed.Zed

##############################
###    APP KEYBINDINGS     ###
##############################

# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -m window -o /home/yusqohid/Pictures/Screenshots/
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output -o /home/yusqohid/Pictures/Screenshots/
# Screenshot a region
bind = SHIFT, PRINT, exec, hyprshot -m region -o /home/yusqohid/Pictures/Screenshots/
